models:
  supervisor:
    endpoint: "${VLLM_BASE_URL}"
    model: "llama-3.3-70b-versatile"
    mock: false

  coder:
    endpoint: "${VLLM_BASE_URL}"
    model: "llama-3.3-70b-versatile"
    mock: false

  security:
    endpoint: "${VLLM_BASE_URL}"
    model: "llama-3.3-70b-versatile"
    mock: false

  perf:
    endpoint: "${VLLM_BASE_URL}"
    model: "llama-3.3-70b-versatile"
    mock: false

  deploy:
    endpoint: "${VLLM_BASE_URL}"
    model: "llama-3.1-8b-instant"
    mock: false
